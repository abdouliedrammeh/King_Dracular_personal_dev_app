from db import add_counter, increment_counter class counter: def __init__(self, name: str, description: str): ''' counter class, to count event print name: the name of the counter print description: descrip the description counter ''' self.name = name self.description = description self.count = 0 def increment(self): self.count += 1 def reset(self): self.count = 0 def __str__(self): return f"{self.name}: {self.count}" - def store (self, db): add_counter(db, self.name, self.description) def add_event (self, db, date: str = None): increment.counter(db, self.name, date) what is wrong with code

